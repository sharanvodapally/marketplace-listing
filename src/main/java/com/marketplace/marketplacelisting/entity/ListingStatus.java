package com.marketplace.marketplacelisting.entity;

public enum ListingStatus {
	
	// user attributeconverter if want "pending pickup" as enum --> pendingpickup("pending pickup"

	Available, PendingPickup, Sold
}
